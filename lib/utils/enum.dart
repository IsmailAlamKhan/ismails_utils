enum FutureStatus { LOADING, SUCCESS, ERROR, Empty, IDLE }
enum ScreenSize {
  SmallPhone,
  MediumPhone,
  LargePhone,
  XLargePhone,
  SmallTab,
  MediumTab,
  LargeTab,
  XLargeTab,
  NONE
}

enum CategoryPageIndex {
  Ticks,
  UTV,
  Permanent,
  League,
  Group,
  Events,
  Shopping,
  UNK,
}

enum ScrollableListCategory { Ticks, Permanent, UTV, EVENTS }

enum SectionType { Small, Large, Circle }

enum SvgType { Asset, String, Network }

enum ButtonType { Elevated, Text, Outline, TextNonRipple }

enum PrintType { Error, Success, Info }

enum NotificationType { Error, Success, Info }
